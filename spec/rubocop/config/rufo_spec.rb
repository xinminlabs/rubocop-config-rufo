RSpec.describe Rubocop::Config::Rufo do
  it "has a version number" do
    expect(Rubocop::Config::Rufo::VERSION).not_to be nil
  end

  it "does something useful" do
    expect(false).to eq(true)
  end
end
